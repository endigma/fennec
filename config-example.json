{
    "port": "1234",
    "handlers": [
        {
            "path": "/example1",

            // Absolute paths only! (I mean relative might work but inconsistently)
            "run": "/scripts/example1.sh"
        },
        {
            "path": "/example2",
            "run": "/scripts/example2.sh"
        }
    ]
}